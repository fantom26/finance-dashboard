# Install deps with Yarn, run lint/format check and production build on Node 22.x.
# json-server@1 requires Node >=22.12 — keep CI on that baseline.
# See: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --frozen-lockfile
        env:
          HUSKY: '0'
      - run: yarn lint
      - run: yarn format:check
      - run: yarn typecheck
      - run: yarn build
